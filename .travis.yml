language: php

php:
  - 7.1
  - 7.2

before_install:
  - sudo apt-get update
  - pecl install -f v8js-0.1.3
  - phpenv config-add travis.php.ini
  - travis_retry composer self-update

install:
  - travis_retry composer install --no-interaction --prefer-source